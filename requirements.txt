# Core app
streamlit>=1.35.0
requests>=2.31.0
pandas>=2.0.0

# AI (Gemini) - direct SDK (recommended)
google-genai>=0.3.0

# Local dev only (optional)
python-dotenv>=1.0.0

# Testing / quality (dev)
pytest>=8.0.0
pytest-cov>=5.0.0
ruff>=0.4.0
pre-commit>=3.7.0
